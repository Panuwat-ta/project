# Core automate deps (แยกจาก server เพื่อรันเบาๆ ได้)
pytest>=8.0
pytest-asyncio>=0.23
pytest-html>=4.1
pytest-cov>=6.0
httpx>=0.27
anyio>=4.0
pyyaml>=6.0
python-dotenv>=1.0
faker>=24.0
requests>=2.32
Pillow>=10.2
# ดึง app จาก server โดยตรงต้องมี deps ของ server อยู่แล้ว
# ถ้ารันแบบ isolated ให้ pip install -r ../server/requirements.txt เพิ่ม
fastapi
sqlalchemy[asyncio]
pydantic
python-jose[cryptography]
passlib
bcrypt==3.2.2
# Performance
locust>=2.20
