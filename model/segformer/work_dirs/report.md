# Segformer Model Training Report (Scam Image Detection)

รายงานฉบับนี้สรุปผลลัพธ์ที่ดีที่สุด (Best Validation mIoU) ของโมเดล Segformer ทั้ง 3 เวอร์ชันที่ถูกเทรนสำหรับโปรเจคตรวจสอบรูปภาพหลอกลวง (Scam Image Detection)

## ตารางเปรียบเทียบผลลัพธ์ (Evaluation Results)

| ลำดับ | เวอร์ชันโมเดล (Model Version) | ค่า mIoU ที่ดีที่สุด (%) | รอบที่ทำได้ (Iteration) |
|-------|-------------------------------|---------------------------|------------------------|
| 1 | **v1.0.0** | **72.42** | 112,000 |
| 2 | `v1.0.1` | 75.48 (มีปัญหา Test Pipeline)* | 152,000 |
| 3 | `segformer_v2.0.0(test-model)` | 71.14 | 144,000 |
| 4 | `v1.0.2` | 67.99 (มีปัญหา Test Pipeline)* | 132,000 |
| 5 | `segformer_v1.0.0(test-model)` | 56.50 | 128,000 |

## สรุปผล (Conclusion)
จากการประเมินและทดสอบการใช้งานจริง โมเดลเวอร์ชัน **v1.0.0** ยังคงเป็นโมเดลที่ให้ผลลัพธ์แม่นยำและเสถียรที่สุดในตอนนี้ (72.42%) ดังนั้นหากต้องการนำโมเดลไปใช้งานจริง (Production) แนะนำให้ใช้ไฟล์ Checkpoint ของโมเดลเวอร์ชันนี้ครับ (`work_dirs/v1.0.0/best_mIoU_iter_112000.pth`)

> **หมายเหตุ:** *ผลลัพธ์ของเวอร์ชัน `v1.0.1` และ `v1.0.2` เกิดขึ้นในระหว่างที่มีข้อผิดพลาดในการตั้งค่าลำดับการประมวลผลหน้ากาก (Test Pipeline) ทำให้คะแนน mIoU คลาดเคลื่อนจากความเป็นจริงและไม่สามารถนำมาใช้เปรียบเทียบได้

---

## ประวัติและวิธีการเทรนแต่ละเวอร์ชัน (Training History & Configurations)

เพื่อความชัดเจนในการติดตามการพัฒนาโมเดล ตารางด้านล่างสรุปชุดข้อมูล (Dataset), ไฟล์การตั้งค่า (Config) และวิธีการเทรนที่ใช้ในแต่ละเวอร์ชัน:

| เวอร์ชัน (Version) | ไฟล์ Config ที่ใช้งาน | ชุดข้อมูล (Dataset) | วิธีการเทรน (Training Method) |
|:-------------------|:----------------------|:-------------------|:------------------------------|
| `segformer_v1.0.0(test-model)` | `segformer_mit-b2.py` | CASIA 2.0 (โฟลเดอร์ `data/`) | ทดสอบระบบครั้งแรก (Test run) เริ่มจาก Scratch |
| `segformer_v2.0.0(test-model)` | `segformer_mit-b2-v1.py` | CASIA 2.0 (โฟลเดอร์ `data/`) | ปรับจูนพารามิเตอร์เบื้องต้น เริ่มจาก Scratch |
| **`v1.0.0`** | `segformer_mit-b2-v2.py` | CASIA 2.0 (โฟลเดอร์ `dataset/`) | **เทรนใหม่จาก Scratch** (เริ่มจาก MiT-B2 ImageNet Pretrained) |
| `v1.0.1` | `segformer_mit-b2-v3.py` | Defacto Inpainting อย่างเดียว | **Fine-tune** โดยโหลด Checkpoint ต่อจาก `v1.0.0` |
| `v1.0.2` | `segformer_mit-b2-v5.py` | CASIA 2.0 + Defacto Inpainting | **Fine-tune** โดยโหลด Checkpoint ต่อจาก `v1.0.0` (ช่วงที่ใช้ชื่อโฟลเดอร์ชุดข้อมูลเก่า `DEFACTOdefacto-inpainting`) |
| **`v1.0.3`** | `segformer_mit-b2-v5.py` | CASIA 2.0 + Defacto Inpainting | **Fine-tune** โดยโหลด Checkpoint ต่อจาก `v1.0.0` (อัปเดตชื่อโฟลเดอร์ใหม่เป็น `defacto-inpainting` โดยผลลัพธ์และข้อมูลคือชุดเดียวกับ v1.0.2) |

**หมายเหตุเพิ่มเติมสำหรับอนาคต (v1.0.4 เป็นต้นไป)**:
จากการทดลองแบบ Fine-tune ในช่วง v1.0.1 ถึง v1.0.3 พบปัญหาเรื่อง Bias และ Catastrophic Forgetting ดังนั้นในการเทรนครั้งถัดไปที่ต้องใช้ข้อมูล Defacto หลายชุดรวมกัน (เช่น Inpainting, Copy-Move, Splicing) ควรกลับไปใช้วิธี **เทรนใหม่จาก Scratch (เริ่มนับ 1 ใหม่)** เพื่อให้โมเดลหาสมดุลที่ดีที่สุดได้ครับ
