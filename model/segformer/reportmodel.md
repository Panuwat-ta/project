# รายงานผลการเปรียบเทียบประสิทธิภาพโมเดล SegFormer

## สรุปผลการประเมิน (Evaluation Results)

ตารางด้านล่างแสดงผลลัพธ์ที่ดีที่สุด (Best Validation mIoU) จากการเทรนในแต่ละเวอร์ชัน โดยเรียงตามประสิทธิภาพ:

| ลำดับ | เวอร์ชันโมเดล (Model Version) | ค่า mIoU สูงสุด (%) | รอบที่บันทึกโมเดล (Iteration) | หมายเหตุ |
|-------|-------------------------------|---------------------|-------------------------------|----------|
| 1 | **v1.0.4** | **86.38** | 495,000 | **โมเดลที่ดีที่สุด ณ ปัจจุบัน** (`best_mIoU_iter_495000.pth`) |
| 2 | `v1.0.1` | 75.48 | 152,000 | *มีปัญหา Test Pipeline คะแนนคลาดเคลื่อน |
| 3 | **v1.0.0** | **72.42** | 112,000 | มีความเสถียรสูงก่อนหน้า v1.0.4 |
| 4 | `segformer_v2.0.0(test-model)` | 71.14 | 144,000 | ปรับจูนเริ่มต้น |
| 5 | `v1.0.2` | 67.99 | 132,000 | *มีปัญหา Test Pipeline คะแนนคลาดเคลื่อน |
| 6 | `segformer_v1.0.0(test-model)` | 56.50 | 128,000 | ทดสอบระบบครั้งแรก |

> **หมายเหตุ:** *ผลลัพธ์ของเวอร์ชัน `v1.0.1` และ `v1.0.2` เกิดขึ้นในระหว่างที่มีข้อผิดพลาดในการตั้งค่าลำดับการประมวลผลหน้ากาก (Test Pipeline) ทำให้คะแนน mIoU คลาดเคลื่อนจากความเป็นจริงและไม่สามารถนำมาใช้เปรียบเทียบได้อย่างแม่นยำ

---

## ประวัติและวิธีการเทรนแต่ละเวอร์ชัน (Training History & Configurations)

เพื่อความชัดเจนในการติดตามการพัฒนาโมเดล ตารางด้านล่างสรุปชุดข้อมูล (Dataset), ไฟล์การตั้งค่า (Config) และวิธีการเทรนที่ใช้ในแต่ละเวอร์ชัน:

| เวอร์ชัน | ไฟล์ Config ที่ใช้งาน | ชุดข้อมูล (Dataset) | วิธีการเทรน (Training Method) |
|:---------|:----------------------|:-------------------|:------------------------------|
| `segformer_v1.0.0(test-model)` | `segformer_mit-b2.py` | CASIA 2.0 | ทดสอบระบบครั้งแรก (Test run) เริ่มจาก Scratch |
| `segformer_v2.0.0(test-model)` | `segformer_mit-b2-v1.py` | CASIA 2.0 | ปรับจูนพารามิเตอร์เบื้องต้น เริ่มจาก Scratch |
| **`v1.0.0`** | `segformer_mit-b2-v2.py` | CASIA 2.0 | **เทรนใหม่จาก Scratch** (เริ่มจาก MiT-B2 ImageNet Pretrained) |
| `v1.0.1` | `segformer_mit-b2-v3.py` | Defacto Inpainting อย่างเดียว | **Fine-tune** โดยโหลด Checkpoint ต่อจาก `v1.0.0` |
| `v1.0.2` | `segformer_mit-b2-v5.py` | CASIA 2.0 + Defacto Inpainting | **Fine-tune** โดยโหลด Checkpoint ต่อจาก `v1.0.0` (โฟลเดอร์ชุดข้อมูลเก่า) |
| **`v1.0.3`** | `segformer_mit-b2-v5.py` | CASIA 2.0 + Defacto Inpainting | **Fine-tune** ต่อจาก `v1.0.0` (อัปเดตชื่อโฟลเดอร์ใหม่) |
| **`v1.0.4`** | `segformer_mit-b2-v7.py` | (อัปเดตชุดข้อมูลล่าสุด) | เทรนด้วยรอบที่สูงขึ้น (500,000 iterations) เพื่อให้ได้ความแม่นยำสูงสุด |


