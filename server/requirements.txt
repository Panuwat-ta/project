# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic[email]>=2.0
pydantic-settings>=2.0

# Database
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30.0
alembic>=1.14.0

# Auth & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# bcrypt pinned: passlib is incompatible with bcrypt >= 4.1 (version-parsing error).
# Keep on 3.2.2 until passlib is replaced or fixed.
bcrypt==3.2.2

# Redis
redis[hiredis]>=5.0

# Image Processing
Pillow>=10.2,<11
python-multipart>=0.0.9
# HEIC support for iPhone uploads (registers Pillow opener).
# Pillow pinned <11 to stay compatible with surya-ocr.
pillow-heif>=0.18.0,<0.19

# AI Inference
# torch ต้องเป็น build cu126 (CUDA 12) ให้ตรงกับ onnxruntime-gpu.
# ถ้าปล่อยให้ pip ดึงจาก PyPI จะได้ cu130 (CUDA 13) -> ขัดกับ onnxruntime -> GPU ล่ม
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.13.0+cu126
torchvision==0.28.0+cu126
onnxruntime-gpu[cuda,cudnn]>=1.18.0
numpy>=1.26,<2
surya-ocr==0.5.0
transformers==4.41.2

# EXIF
piexif>=1.1.3

# Rate Limiting
slowapi>=0.1.9

# Testing
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
opencv-python-headless>=4.10.0.84
