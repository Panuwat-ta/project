# ตัวอย่าง fixture สำหรับ build.py (Loop 2)
# ครอบคลุม: H1 หนึ่งตัว + wikilink หนึ่งตัว + ตารางหนึ่งตาราง + fenced code หนึ่งบล็อก

# Fixture หน้าเอกสาร

หน้านี้ลิงก์ไป [[overview|ภาพรวม]] ของเว็บจริง

| มิติ | ช่วงคะแนน |
| :--- | :--- |
| ข้อความ | 0–100 |

```python
print("fixture ok")
```
