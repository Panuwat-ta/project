# Archive — รายงานของสิ่งที่เลิกใช้แล้ว

- `risk_calculator_weights.md` — ผลรันของสูตร Weighted เดิม (Textual 20% / Visual 60% / Source 20%) ซึ่งถูกแทนด้วยสูตร Hybrid ทางการแล้ว (`S_base = max(...)`, +5 ต่อมิติรองที่ >= 40, Visual Override เมื่อ Visual >= 80)
- คงไฟล์ไว้เพื่ออ้างอิงประวัติเท่านั้น ห้ามใช้เป็นเกณฑ์ตรวจรับ — สูตรทางการดูที่ `tests_all/rtm.md` และ SRS ปัจจุบัน
