## 2026-09-18 11:00 +07 - [Promote SegFormer v1.0.6 to Production]

- Target: `server/.env` (`ONNX_MODEL_PATH`), `POST /api/v1/admin/models/14/deploy`, live scan `POST /api/v1/scan/`
- Command: `./tests_model/add-v-mode.sh` (ลงทะเบียนแล้วก่อนหน้า), `POST /api/v1/admin/models/14/dry-run`, `POST /api/v1/admin/models/14/deploy`, สแกนจริง 3 ภาพผ่าน API
- Result: PASS (deploy สำเร็จ, `v1.0.6` active)
- Summary: Total: parity 2 + dry-run 1 + deploy 1 + live scan 3 | Passed: 7 | Failed: 0 | Skipped: 0 | Duration: parity ~2 นาที + deploy/scan ~5 นาที (ไม่รวมสตาร์ท DB/server)

### 1. รายการที่ผ่าน (Passed Tests) และพฤติกรรมที่ผ่าน (How it Passed)

- **[PyTorch–ONNX numerical parity (CPU-vs-CPU)]**:
  - พฤติกรรมที่ผ่าน: ป้อน normalized tensor ก้อนเดียวกันจากภาพ `tests_model/img/test2.png` ให้ PyTorch checkpoint และ ONNX ได้ logits shape ตรงกัน `(1,2,128,128)` ค่า max abs diff 8.3e-06 ต่ำกว่าเกณฑ์ 1e-04 ยืนยันว่า export ถูกต้อง exact ส่วนต่าง 2.5e-03 ที่พบตอนเทียบ CUDA-vs-CPU เป็น kernel noise ข้าม device ซึ่งกระทบ forgery probability เพียง 1.9e-04 (เปลี่ยนคะแนน int 0-100 ไม่ได้)
- **[Admin dry-run (`models/14/dry-run`)]**:
  - พฤติกรรมที่ผ่าน: API ตอบ `success: true` ว่า `Model v1.0.6 is ready for deployment` พร้อม latency 4303ms, memory 202MB, compatibility Passed, test prediction Score 99%
- **[Admin deploy (`models/14/deploy`)]**:
  - พฤติกรรมที่ผ่าน: API ตอบ `is_active: true`, `status: active`, `deployed_at` 2026-09-18T03:33:25Z พร้อม audit log `deploy_model` และ `deployment_history` รุ่นก่อนหน้า (`v1.0.5`) ถูก set inactive อัตโนมัติ `.env` ถูกเขียนเป็น path `v1.0.6` โดย `_switch_serving_model`
- **[Live scan ภาพปกติ (`tests/test1.png`)]**:
  - พฤติกรรมที่ผ่าน: สแกนผ่าน API จบ `completed` ใน ~6 วินาที ได้ visual 15 / total 20 / grade low / heatmap ถูกบันทึก พร้อม OCR และคำอธิบาย XAI ภาษาไทย
- **[Live scan ภาพปลอมที่รู้คำตอบ (splicing `tp_splice1_10007`) ผ่าน worker ตรง]**:
  - พฤติกรรมที่ผ่าน: ได้ visual 100 / ai_gen 0.9999 / region ส่วนบนของภาพ ตรวจจับรอยปลอมได้ถูกต้อง
- **[Live scan หลัง deploy (`tests/test2.png`, ล้าง cache แล้ว)]**:
  - พฤติกรรมที่ผ่าน: คำนวณใหม่ด้วย `v1.0.6` ได้ visual 13 / ai_gen 0.1255 / heatmap ถูกบันทึก ตรงกับผล worker ตรง (0.1256)
- **[ล้าง Redis `scan_result:*` ค้างเก่า]**:
  - พฤติกรรมที่ผ่าน: ลบ 7 keys ที่ค้างจากโมเดลเก่า (TTL สูงสุด 30 วัน) เพื่อไม่ให้เสิร์ฟคะแนนโมเดลเก่าหลังเปลี่ยนรุ่น

### 2. รายการที่ไม่ผ่าน (Failed Tests) และสาเหตุที่ไม่ผ่าน (How & Why it Failed)

ไม่มีข้อผิดพลาด (0 Failed)

ข้อสังเกตที่ต้องแจ้ง (ไม่ใช่ failure แต่มีผลต่อการใช้งาน):
- สแกน `tests/test2.png` (ภาพคนวิ่งหนีจระเข้) ด้วย `v1.0.6` ได้ visual 13 ขณะที่ cache เก่าของโมเดลรุ่นก่อนให้ 98 ปรากฏการณ์นี้ตรงกับที่บันทึกไว้ใน `reportmodel.md` ว่าภาพ demo จำนวนน้อย (n=3, ไม่มี ground truth) ไม่ใช่ benchmark และสรุปความสามารถทั่วไปไม่ได้ คะแนนทางการ (locked common test Forgery IoU 90.26) ยังยืนยันว่า `v1.0.6` ดีที่สุด หากภาพจริงมีปัญหาสามารถ rollback ไป `v1.0.5` (id 12) ผ่าน endpoint เดียวกันได้ทันที
- `tests/api/test_scan_xai_live.py` รันไม่ผ่านเพราะไฟล์ `tests/test.png` ไม่มีใน repo (ปัญหาค้างเดิม ไม่เกี่ยวกับงานนี้) จึง verify ด้วยสแกนจริงผ่าน API แทนตามขั้นตอนข้างต้น
- สร้างผู้ใช้ทดสอบ `prodverify@gmail.com` (id 81) พร้อมสแกน 3 รายการใน DB จริงเพื่อ verify ควรลบออกหากไม่ต้องการ
