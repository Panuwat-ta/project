# Test Cases: ชุดทดสอบภาพ Test-Cases (SegFormer Forgery Localization)

- **Test Data Root**: `/home/panuwat/Pictures/Test-Cases/`
- **แบบมี mask** (`with_mask/`, 105 คู่, mask 0/1): casia 15 / authentic 15 / splicing 15 / inpainting 15 / copymove 15 / face 15 / imd2020 15
- **แบบคู่เทียบ** (`pairs/`, 30 คู่, ไม่มี mask): `originals/pair001–030` + `manipulated/pair001–030` + `pairs.json`
- **ชุดดูอย่างเดียว** (`/home/panuwat/Pictures/test-model/with_mask/`, mask 0/255): ไฟล์เดียวกัน ดูด้วย image viewer
- **ฟอร์แมตตาราง**: ตาม `.agents/AGENTS.md` section 9 (10 คอลัมน์, อิง IEEE 829 / ISTQB)

| Test Case ID | Test Case name | Requirement ID | Preconditions | Test Data | Description | Test Steps | Expected Results | Actual Result / Status | Priority |
|---|---|---|---|---|---|---|---|---|---|
| TC-IMGM-01 | Mask test - success - detect forgery with ground-truth mask (CASIA) | FR-SYS-05 | 1. ONNX `segformer_v1_0_5_dynamic.onnx` โหลดได้ <br> 2. `Test-Cases/with_mask/` อยู่ครบ | image = `with_mask/casia/images/test/tp_casia_Tp_D_CNN_M_N_txt00054_txt00050_10375_tif.png` <br> mask = `with_mask/casia/annotations/test/tp_casia_Tp_D_CNN_M_N_txt00054_txt00050_10375_tif.png` (0/1) | 1. Image/mask ต้องเป็นคู่กัน (ชื่อไฟล์ตรงกัน) <br> 2. mask มีแค่ค่า 0/1 <br> 3. mask ต้องมี pixel = 1 (มีจุดปลอมจริง) | 1. โหลด image + mask เข้า BaseSegDataset (`img_suffix='.png'`) <br> 2. รัน SegFormer ONNX ได้ probability map <br> 3. threshold 0.5 แล้วเทียบกับ mask คำนวณ IoU คลาส forgery | 1. โหลดสำเร็จ ไม่ error `need at least one array to concatenate` <br> 2. forgery IoU > 0 (เจอจุดปลอม ไม่ใช่ทาย background ล้วน) <br> 3. ค่า mask ที่อ่านได้มีแค่ 0/1 | To Do | High |
| TC-IMGM-02 | Mask test - success - authentic image must give blank mask (negative) | FR-SYS-05 | 1. ONNX โหลดได้ <br> 2. `Test-Cases/with_mask/` อยู่ครบ | image = `with_mask/authentic/images/test/au_usb_000000000036_jpg.png` <br> mask = `with_mask/authentic/annotations/test/au_usb_000000000036_jpg.png` | 1. รูปจริง mask ต้องดำล้วน (ค่า 0 ทั้งหมด) <br> 2. ใช้เป็นเคส negative กัน false positive | 1. โหลด image + mask <br> 2. รัน ONNX ได้ probability map <br> 3. นับ pixel ที่ prob > 0.5 | 1. mask ที่อ่านได้มีแค่ค่า 0 (ไม่มี 255 หลุด) <br> 2. pixel ที่โมเดลทายว่าปลอมต้องน้อย (false positive ต่ำ) <br> 3. ถ้าโมเดลทายปลอมทั้งภาพ = FAIL (โมเดลหลอน) | To Do | High |
| TC-IMGM-03 | Mask test - success - face morphing detection | FR-SYS-05 | 1. ONNX โหลดได้ <br> 2. `Test-Cases/with_mask/` อยู่ครบ | image = `with_mask/face/images/test/tp_face_morphing_0.jpg_IN_100.jpg_tif.png` <br> mask = `with_mask/face/annotations/test/tp_face_morphing_0.jpg_IN_100.jpg_tif.png` (0/1) | 1. ตรวจประเภทย่อย face morphing (ไม่ใช่ splicing) <br> 2. mask มีค่า 0/1 | 1. โหลด image + mask <br> 2. รัน ONNX + threshold 0.5 <br> 3. เทียบตำแหน่งจุดขาวกับ heatmap | 1. โหลดสำเร็จ <br> 2. heatmap ติดบริเวณใบหน้า (ตรง mask) <br> 3. forgery IoU > 0 | To Do | Medium |
| TC-IMGP-01 | Pair test - success - human-edited photo must trigger heatmap (PS-Battles) | FR-SYS-08 | 1. ONNX โหลดได้ <br> 2. `pairs/pairs.json` อ่านได้ | original = `pairs/originals/pair001_orig.jpg` <br> manipulated = `pairs/manipulated/pair001_mani.jpg` <br> mapping = `pairs/pairs.json` (pair001) | 1. ไม่มี mask ใช้ดูด้วยตาเท่านั้น <br> 2. คู่ต้องตรงกันผ่าน pairs.json (ห้ามสลับคู่) | 1. เปิด pairs.json ยืนยัน pair001 ผูก original/manipulated ถูกคู่ <br> 2. รัน ONNX บน manipulated ดู heatmap <br> 3. รัน ONNX บน original เทียบ | 1. manipulated มี heatmap ติดบริเวณที่ถูกตัดต่อ <br> 2. original heatmap ต่ำกว่าชัดเจน <br> 3. ถ้า original ติดแรงกว่า manipulated = FAIL (คู่สลับหรือโมเดลผิด) | To Do | Medium |
| TC-IMGP-02 | Pair test - fail - mismatched pair must be rejected | FR-SYS-08 | 1. `pairs/pairs.json` อ่านได้ | original = `pairs/originals/pair001_orig.jpg` <br> manipulated = `pairs/manipulated/pair002_mani.png` (ผิดคู่) | 1. เอารูปคนละคู่มาจับคู่กัน <br> 2. ระบบ/คนตรวจต้องจับได้ว่าคู่ไม่ตรง | 1. เทียบชื่อไฟล์กับ pairs.json (pair001 vs pair002) <br> 2. ตรวจว่า mapping ไม่ตรง | 1. ระบบต้อง REJECT (mapping ไม่ตรงกัน) <br> 2. ห้ามนำผลคู่นี้ไปบันทึกเป็นผลทดสอบเด็ดขาด | To Do | High |
| TC-IMGM-04 | Mask test - fail - image without paired mask must be skipped | FR-SYS-05 | 1. BaseSegDataset import ได้ | image = รูปที่ไม่มี mask ชื่อเดียวกันใน `annotations/test/` | 1. เลียนแบบเคสบั๊ก `img_suffix` (default หา .jpg แต่ไฟล์จริง .png → dataset ว่าง) <br> 2. loader ต้องข้าม ไม่แครช | 1. ตั้ง `img_suffix` ผิด (เช่น `.jpg` ทั้งที่ไฟล์เป็น `.png`) <br> 2. build BaseSegDataset | 1. ต้องได้ dataset ว่างหรือข้ามไฟล์ ไม่ใช่ค่าเงียบ <br> 2. ห้าม error `need at least one array to concatenate` กลางทางโดยไม่มีข้อความบอกสาเหตุ | To Do | High |
