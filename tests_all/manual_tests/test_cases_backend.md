# ชุดกรณีทดสอบ: ระบบบริการหลังบ้านและฐานข้อมูล (Backend API & Database - FastAPI)

- **System**: ScamGuard Backend Service
- **Framework**: FastAPI (Asynchronous Python 3.10+), SQLAlchemy 2.0 ORM, Pydantic V2
- **Database & Cache**: PostgreSQL 15 (Alpine), Redis 7 (Alpine)
- **Version**: 1.0.0
- **Status**: Baseline

---

## 1. หมวดหมู่การยืนยันตัวตนและการจัดการสิทธิ์ (Authentication & RBAC)

| Test Case ID | Test Case name | Requirement ID | Preconditions | Test Data | Description | Test Steps | Expected Results | Actual Result / Status | Priority |
|---|---|---|---|---|---|---|---|---|---|
| TC-BE-AUTH-01 | การลงทะเบียนผู้ใช้งานใหม่ผ่าน API (User Registration API) | FR-AUTH-01 | 1. PostgreSQL Container และ FastAPI Service ทำงานปกติ | - Endpoint: `POST /api/v1/auth/register`<br>- Payload:<br>{<br>"email": "be_test_user@scamguard.local",<br>"password": "StrongPassword123!",<br>"full_name": "Backend Test User",<br>"system_consent": true,<br>"research_consent": false<br>} | Module / Feature: Auth / Register Endpoint<br>Test Type: Functional / API | 1. ยิง HTTP POST ไปยัง `/api/v1/auth/register` พร้อม JSON Payload<br>2. ตรวจสอบข้อมูลในตาราง `users` และ `consent_logs` ของฐานข้อมูล | 1. ได้รับ HTTP Status Code: `201 Created`<br>2. Response Body มี `id`, `email`, `full_name`, `role` (ไม่มี Plaintext Password คืนกลับมา)<br>3. ในตาราง `users` คอลัมน์ `hashed_password` ต้องเป็นแฮช bcrypt (ขึ้นต้น `$2b$` ยาว 60 ตัวอักษร ไม่ใช่ plaintext) — วิธีตรวจ: `SELECT hashed_password FROM users WHERE email='...';` แล้วตรวจ prefix/ความยาวด้วย `python3 -c "print(len(h))"` (ต้องได้ 60) และยืนยันว่าไม่ตรงกับรหัสผ่านต้นฉบับ<br>4. มีบันทึกความยินยอมในตาราง `consent_logs` ตรงกับค่าที่ส่งมา<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_auth_flow.py` | To Do | P0 (Blocker) |
| TC-BE-AUTH-02 | การเข้าสู่ระบบเพื่อรับ JWT Tokens (Login & Token Generation) | FR-AUTH-02, NFR-SEC-02 | 1. มีผู้ใช้ `be_test_user@scamguard.local` ในระบบ | - Endpoint: `POST /api/v1/auth/login`<br>- Content-Type: `application/x-www-form-urlencoded`<br>- Form fields:<br>- `username`: `be_test_user@scamguard.local` (ใช้อีเมลเป็นชื่อผู้ใช้)<br>- `password`: `StrongPassword123!` | Module / Feature: Auth / Login Endpoint<br>Test Type: Functional / API | 1. ส่งคำขอ Login แบบฟอร์มพร้อมฟิลด์ `username` และ `password` ไปยัง Backend | 1. ได้รับ HTTP Status Code: `200 OK`<br>2. Response Body ส่งคืนโครงสร้าง DTO:<br>{<br>"access_token": "***...",<br>"refresh_token": "***...",<br>"token_type": "bearer",<br>"user": {<br>"id": 1,<br>"email": "be_test_user@scamguard.local",<br>"full_name": "Backend Test User",<br>"role": "user"<br>}<br>}<br>3. Access Token ถอดรหัสได้ `sub` ตรงกับ User ID และมีค่า `exp` กำหนดวันหมดอายุ<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_auth_flow.py` | To Do | P0 (Blocker) |
| TC-BE-AUTH-03 | การต่ออายุโทเค็น (Refresh Token Flow) | FR-AUTH-04 | 1. มี Valid Refresh Token จากการล็อกอิน | - Endpoint: `POST /api/v1/auth/refresh`<br>- Content-Type: `application/json`<br>- Payload:<br>{<br>"refresh_token": "<valid_refresh_token>"<br>} | Module / Feature: Auth / Refresh Token Endpoint<br>Test Type: Functional / Security | 1. ส่งคำขอพร้อม JSON ที่มีฟิลด์ `refresh_token` เพื่อขอ Access Token ชุดใหม่ | 1. ได้รับ HTTP Status Code: `200 OK`<br>2. ได้รับ `access_token` ใหม่ที่มีอายุการใช้งานนับจากเวลาปัจจุบัน<br>3. ไม่จำเป็นต้องให้ผู้ใช้กรอกรหัสผ่านใหม่<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_auth_flow.py` | To Do | P1 (High) |
| TC-BE-AUTH-04 | การปฏิเสธคำขอเมื่อ Token หมดอายุหรือไม่ถูกต้อง (Unauthorized Access) | NFR-SEC-02 | 1. เตรียม Expired Token หรือ Invalid Token | - Endpoint: `GET /api/v1/auth/me`<br>- Headers: `Authorization: Bearer invali...ken` | Module / Feature: Auth / Token Validation Middleware<br>Test Type: Security / Negative | 1. ยิงคำขอไปยัง Protected Endpoint | 1. ระบบตอบกลับด้วย HTTP Status Code: `401 Unauthorized`<br>2. Response Body: `{"detail": "Could not validate credentials"}` สำหรับ Token ไม่ถูกต้องหรือหมดอายุ หรือ `{"detail": "Invalid or expired refresh token"}` สำหรับการต่ออายุด้วย Refresh Token ที่ใช้ไม่ได้<br>3. ไม่สามารถเข้าถึงข้อมูลเบื้องหลังได้<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_auth_flow.py` | To Do | P0 (Blocker) |
| TC-BE-AUTH-05 | การดึงโปรไฟล์และการออกจากระบบ (Me Plus Logout) | FR-AUTH-02, FR-AUTH-05 | 1. มี Access Token ที่ยังไม่หมดอายุ | - Endpoint: `GET /api/v1/auth/me`<br>- Endpoint: `POST /api/v1/auth/logout`<br>- Headers: `Authorization: Bearer <valid...en>` | Module / Feature: Auth / Profile and Logout<br>Test Type: Functional | 1. เรียก `GET /api/v1/auth/me` พร้อม Token ที่ถูกต้อง<br>2. เรียก `POST /api/v1/auth/logout` พร้อม Token เดียวกัน | 1. `GET /me` ตอบ `200 OK` พร้อม `id`, `email`, `full_name`, `role`<br>2. `POST /logout` ตอบ `200 OK` พร้อม `{"message": "Successfully logged out"}`<br>3. ฝั่ง Server ใช้ JWT แบบ Stateless การล้าง Token หลักทำที่ Client<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_auth_flow.py` | To Do | P1 (High) |
| TC-BE-AUTH-06 | การปฏิเสธการเข้าสู่ระบบเมื่อรหัสผ่านผิด (Login Wrong Password) | FR-AUTH-02 | 1. มีผู้ใช้ `be_test_user@scamguard.local` ในระบบ | - Endpoint: `POST /api/v1/auth/login`<br>- Content-Type: `application/x-www-form-urlencoded`<br>- Form fields:<br>- `username`: `be_test_user@scamguard.local`<br>- `password`: `WrongPassword999` | Module / Feature: Auth / Login Error Handling<br>Test Type: Negative | 1. ส่งคำขอ Login แบบฟอร์มโดยกรอกอีเมลถูกแต่รหัสผ่านผิด | 1. ได้รับ HTTP Status Code: `401 Unauthorized`<br>2. Response Body: `{"detail": "Incorrect email or password"}`<br>3. ไม่มีการออก Token ใดๆ<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_auth_flow.py` | To Do | P1 (High) |

---

## 2. หมวดหมู่การอัปโหลดและตรวจสอบไฟล์รูปภาพ (Scan & File Validation API)

| Test Case ID | Test Case name | Requirement ID | Preconditions | Test Data | Description | Test Steps | Expected Results | Actual Result / Status | Priority |
|---|---|---|---|---|---|---|---|---|---|
| TC-BE-SCAN-01 | การอัปโหลดภาพและรับผลการวิเคราะห์ (Normal Scan Flow) | FR-INPUT-03, FR-SYS-07 | 1. มี Bearer Token ที่ถูกต้องของผู้ใช้<br>2. รูปภาพตัวอย่างที่ถูกต้องตามมาตรฐาน (เช่น `sample.jpg`, ขนาด 500 KB) | - Endpoint: `POST /api/v1/scan/`<br>- Content-Type: `multipart/form-data`<br>- File: `sample.jpg`<br>- Field: `file` พร้อม `title` (ถ้ามี) | Module / Feature: Scan / Image Scan Endpoint<br>Test Type: Integration / API | 1. แนบไฟล์รูปภาพใน Form Field `file`<br>2. ส่งคำขอพร้อม Authorization Header | 1. ได้รับ HTTP Status Code: `200 OK`<br>2. Response Body ประกอบด้วย:<br>- `id`: รหัสการสแกน (UUID v4)<br>- `total_risk_score`: ตัวเลขอัตราความเสี่ยง 0 – 100<br>- `risk_grade`: ระดับความเสี่ยงตัวพิมพ์เล็ก (`low`, `medium`, `high`)<br>- `text_score`, `visual_score`, `source_score`: คะแนนจำแนกรายด้าน<br>- `heatmap_image_url`: URL สำหรับดาวน์โหลดภาพ Heatmap<br>- `status` และ `progress`: สถานะงานปัจจุบัน<br>3. ข้อมูลถูกบันทึกลงในตาราง `scans` ของ PostgreSQL พร้อมเวลาที่บันทึก<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_scan_workflow.py` | To Do | P0 (Blocker) |
| TC-BE-SCAN-02 | การปฏิเสธไฟล์ที่มีขนาดเกินขีดจำกัด Server 20MB (Server-side File Size Enforcement) | FR-INPUT-04 | 1. เตรียมไฟล์ภาพจำลองขนาด 21 MB (เกินลิมิตฝั่ง Server 20MB; ฝั่ง Mobile ไม่ปฏิเสธไฟล์เกิน 10MB แต่แสดงคำเตือนและบีบอัดก่อนส่ง) | - Endpoint: `POST /api/v1/scan/`<br>- File: ขนาด 21 MB | Module / Feature: Scan / Size Enforcement<br>Test Type: Boundary / Negative | 1. ยิงคำขออัปโหลดไฟล์ขนาดเกิน 20MB เข้าสู่ Endpoint | 1. Backend ปฏิเสธคำขอทันทีด้วย HTTP Status Code: `413 Payload Too Large`<br>2. Response Body: `{"detail": "File too large. Maximum allowed size is 20 MB."}`<br>3. หมายเหตุการอ่านไฟล์: เซิร์ฟเวอร์อ่านไฟล์ทั้งหมดเข้าหน่วยความจำก่อน (`await file.read()` ใน `create_scan_task`) แล้วจึงตรวจขนาด ไม่ได้ยกเลิกการอ่าน Stream กลางคัน<br>- **Automation Mapping**: `server/tests/api/test_scan.py` | To Do | P1 (High) |
| TC-BE-SCAN-03 | การตรวจจับ Magic Bytes ป้องกันไฟล์ปลอมแปลงนามสกุล (MIME Spoofing / Magic Bytes) | FR-INPUT-04, NFR-SEC-04 | 1. สร้างไฟล์สคริปต์อันตรายหรือไฟล์ข้อความ แต่เปลี่ยนนามสกุลเป็น `.png` (เช่น ไฟล์ Text ธรรมดาที่ข้างในเป็นสคริปต์ Bash แต่ตั้งชื่อ `malicious.png`) | - Filename: `fake_image.png` (เนื้อหาภายในไม่มี Magic Bytes ของ PNG: `89 50 4E 47`) | Module / Feature: Security / File Sanitization<br>Test Type: Security / Negative | 1. อัปโหลดไฟล์ดังกล่าวเข้าสู่ API สแกนภาพ จดค่า `scan_id` (หรือ `id`) จาก Response (`200 OK` + สถานะ `uploading`)<br>2. Poll `GET /api/v1/scan/{scan_id}` ทุก 5 วินาที นานสูงสุด 60 วินาที จนกว่าสถานะเป็น `failed` (วิธีทำซ้ำ: รัน 3 รอบ ต้องได้ `failed` ทั้ง 3 รอบ; ครบ 60 วินาทีแล้วยังไม่ failed ถือว่า FAIL) | 1. `POST /api/v1/scan/` ตอบ `200 OK` พร้อมสถานะ `uploading` ก่อน ไม่ตรวจ Magic Bytes ทันทีตอนอัปโหลด (การตรวจอยู่ในงานเบื้องหลังผ่าน `load_image_verified` ซึ่งตรวจ Magic Bytes โดยไม่เชื่อ `content_type`)<br>2. งานเบื้องหลังตรวจพบว่าเนื้อหาไม่ใช่รูปภาพจริง สถานะงานเป็น `failed` เมื่อเรียกดูด้วย `GET /api/v1/scan/{scan_id}` ฝั่งแอปรับเป็น `ScanError` พร้อมข้อความทั่วไป (ข้อความรายละเอียด `File is not a valid image.` มีอยู่ใน `image_utils.py` แต่ไม่ถูกส่งกลับถึง Client ในโฟลว์ปัจจุบัน)<br>3. ไฟล์ไม่ถูกบันทึกเป็นหลักฐานและไม่ถูกส่งต่อไปยัง Subprocess หรือโมเดล AI (การบันทึกไฟล์เกิดหลังตรวจผ่านเท่านั้น) ป้องกันช่องโหว่ RCE และ Memory Corrupt<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_scan_workflow.py` | To Do | P0 (Blocker) |
| TC-BE-SCAN-04 | การปฏิเสธไฟล์รูปภาพที่เสียหายเปิดไม่ได้ (Corrupt Image) | FR-INPUT-04 | 1. มี Bearer Token ที่ถูกต้อง<br>2. เตรียมไฟล์นามสกุล jpg แต่ข้อมูลภายในเสียหายเปิดไม่ได้ | - Endpoint: `POST /api/v1/scan/`<br>- File: `corrupt.jpg` (ข้อมูลไบต์ไม่สมบูรณ์) | Module / Feature: Scan / Corrupt File Handling<br>Test Type: Negative | 1. อัปโหลดไฟล์เสียหายเข้าสู่ Endpoint สแกน จดค่า `scan_id` จาก Response (`200 OK` + สถานะ `uploading`)<br>2. Poll `GET /api/v1/scan/{scan_id}` ทุก 5 วินาที นานสูงสุด 60 วินาที จนกว่าสถานะเป็น `failed` (วิธีทำซ้ำ: รัน 3 รอบ ต้องได้ `failed` ทั้ง 3 รอบ; ครบ 60 วินาทีแล้วยังไม่ failed ถือว่า FAIL) | 1. `POST /api/v1/scan/` ตอบ `200 OK` พร้อมสถานะ `uploading` ก่อนเช่นเดียวกับ TC-BE-SCAN-03 งานเบื้องหลังตรวจพบว่าไฟล์เสียหาย สถานะงานเป็น `failed` เมื่อเรียกดูด้วย `GET /api/v1/scan/{scan_id}` (ไม่มีการตอบ `400/422` ทันทีตอนอัปโหลดในโฟลว์ปัจจุบัน ข้อความ `Image is corrupted or unsupported.` มีอยู่ใน `image_utils.py` ฝั่งงานเบื้องหลัง)<br>2. ไฟล์ไม่ถูกส่งต่อเข้า AI Pipeline<br>3. ไม่เกิดการแครชของ Worker<br>- **Automation Mapping**: `server/tests/api/test_scan.py` | To Do | P1 (High) |

---

## 3. หมวดหมู่การแคชผลลัพธ์ด้วย Redis (Redis Caching Mechanism)

| Test Case ID | Test Case name | Requirement ID | Preconditions | Test Data | Description | Test Steps | Expected Results | Actual Result / Status | Priority |
|---|---|---|---|---|---|---|---|---|---|
| TC-BE-CACHE-01 | การตรวจสอบแคชพลาดและบันทึกผลลงแคช (Cache Miss Flow) | FR-SYS-09 | 1. ล้างคีย์ใน Redis หรือใช้ภาพใหม่ที่ไม่เคยอัปโหลดมาก่อน | ภาพที่ไม่เคยผ่านระบบมาก่อน | Module / Feature: Cache / SHA-256 Image Hash<br>Test Type: Integration | 1. อัปโหลดภาพเข้าสู่ API<br>2. ตรวจสอบ Key ใน Redis Container | 1. ตรวจไม่พบคีย์ใน Redis (Cache Miss)<br>2. ระบบประมวลผลผ่าน AI Inference Pipeline ปกติ<br>3. เมื่อได้ผลลัพธ์ ระบบคำนวณ SHA-256 Hash ของไฟล์ภาพ และบันทึกผลลัพธ์ลง Redis พร้อมตั้ง TTL<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_scan_workflow.py` | To Do | P1 (High) |
| TC-BE-CACHE-02 | การตอบกลับเร็วเมื่อตรวจพบภาพซ้ำเดิม (Cache Hit Acceleration) | FR-SYS-09, NFR-PERF-01 | 1. เตรียมภาพ A ที่ไม่ซ้ำกับงานอื่น (ตรวจว่าไม่มีคีย์ SHA-256 ของภาพ A ใน Redis ก่อนเริ่ม — วิธีล้าง/เตรียม: `redis-cli DEL <sha256ของภาพA>` หรือใช้ภาพใหม่ที่คำนวณแฮชแล้วไม่พบคีย์) | ภาพ A ไฟล์เดิมเป๊ะ | Module / Feature: Cache / Performance Optimization<br>Test Type: Performance / Integration | 1. (warm cache ใน TC เอง — ห้ามอ้างผลจาก TC-BE-CACHE-01) อัปโหลดภาพ A ครั้งที่ 1 รอจนได้ผลสำเร็จ<br>2. ส่งคำขอสแกนภาพ A ซ้ำอีกครั้ง (ไฟล์เดิมเป๊ะ)<br>3. วัดเวลาตอบสนอง (Response Latency) ของครั้งที่ 2 | 1. ตรวจพบคีย์แฮชใน Redis ทันที (Cache Hit)<br>2. ได้รับ HTTP 200 พร้อมผลลัพธ์การสแกนเดิม<br>3. เวลาในการตอบกลับ P95 ไม่เกิน 3 วินาทีแบบ End-to-End (P95 ≤ 3s ตรงตาม NFR-PERF-01; ภาพ 1MB, RTT ≤50ms) โดยไม่มีการเรียกใช้งานโมเดล AI ใน Subprocess ซ้ำ<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_scan_workflow.py` | To Do | P0 (Blocker) |

---

## 4. หมวดหมู่ความปลอดภัยและการจำกัดอัตราคำขอ (Security & Rate Limiting)

| Test Case ID | Test Case name | Requirement ID | Preconditions | Test Data | Description | Test Steps | Expected Results | Actual Result / Status | Priority |
|---|---|---|---|---|---|---|---|---|---|
| TC-BE-RATE-01 | การจำกัดอัตราคำขอทั่วไปและฝั่ง Admin (Rate Limiting) | NFR-SEC-04 | 1. Server กำหนดโควตาทั่วไว้ที่ 60 ครั้งต่อชั่วโมงต่อ IP และกำหนด Limit ของ `POST /api/v1/admin/login` กับ `POST /api/v1/admin/refresh` ไว้ที่ 5 ครั้งต่อนาที | ยิงคำขอ `POST /api/v1/admin/login` เกิน 5 ครั้งภายใน 1 นาทีจาก IP เดียวกัน | Module / Feature: Security / Rate Limiting Middleware<br>Test Type: Boundary | 1. (โควตา Admin) ใช้ลูปยิง `POST /api/v1/admin/login` เกิน 5 ครั้งภายใน 1 นาทีจาก IP เดียวกัน จดรหัสสถานะและ Header `Retry-After` ของคำขอที่ 6 เป็นต้นไป<br>2. (โควตาทั่ว) ยิงคำขอทั่วไป (เช่น `GET /api/v1/auth/me` ด้วย Token ถูกต้อง) เกิน 60 ครั้งภายใน 1 ชั่วโมงจาก IP เดียวกัน แล้วตรวจว่าคำขอที่ 61+ ได้ `429` พร้อม Header `Retry-After` (หมายเหตุ: หากรันเต็ม 60 ครั้งใช้เวลานาน อนุญาตให้ตรวจด้วย counter/stub ของ middleware ใน env ทดสอบ แล้วบันทึกวิธีไว้ในผลรัน) | 1. คำขอภายในโควตา (5 ครั้งแรกต่อนาที) ไม่ถูกปฏิเสธด้วย 429 — ตอบตามผลข้อมูลจริง (`200 OK` หรือ `401` กรณีข้อมูลผิด) ภายใน 5 วินาที<br>2. คำขอเกินโควตาถูกปฏิเสธด้วย HTTP Status Code: `429 Too Many Requests`<br>3. มี Header `Retry-After` แจ้งเวลาที่ต้องรอก่อนยิงใหม่อีกครั้ง<br>4. โควตาทั่ว 60 ครั้งต่อชั่วโมงมีผลกับทุกคำขอ ส่วนโควตา Admin 5 ครั้งต่อนาทีมีผลเฉพาะเส้นล็อกอินและต่ออายุ Token ฝั่ง Admin<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_admin.py` | To Do | P1 (High) |
| TC-BE-CORS-01 | การตรวจสอบและยอมรับเฉพาะ Origins ที่กำหนด (CORS Policy) | NFR-SEC-01 | 1. ตัวแปร `ALLOWED_ORIGINS` ใน `.env` ระบุเฉพาะโดเมนของ Admin Portal และ Localhost | - Origin ที่อนุญาต: `http://localhost:5173`<br>- Origin แปลกปลอม: `http://malicious-site.com` | Module / Feature: Security / CORS Middleware<br>Test Type: Security | 1. ส่งคำขอ Preflight `OPTIONS /api/v1/auth/login` โดยตั้ง Header `Origin: http://malicious-site.com` | 1. Response จะไม่มี Header `Access-Control-Allow-Origin` ส่งกลับไปให้กับ Origin แปลกปลอม<br>2. เมื่อส่งด้วย Origin ที่อนุญาต จะได้รับ Header `Access-Control-Allow-Origin: http://localhost:5173` ครบถ้วน<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_health.py` | To Do | P1 (High) |

---

## 5. หมวดหมู่การจัดการสิทธิ์และการบริหารผู้ดูแลระบบ (Admin APIs & RBAC)

| Test Case ID | Test Case name | Requirement ID | Preconditions | Test Data | Description | Test Steps | Expected Results | Actual Result / Status | Priority |
|---|---|---|---|---|---|---|---|---|---|
| TC-BE-ADMIN-01 | การปฏิเสธผู้ใช้ทั่วไปเมื่อพยายามเรียกใช้ Admin Endpoints (RBAC Enforcement) | NFR-SEC-03 | 1. มี Token ของผู้ใช้ทั่วไป (Normal User จากตาราง `users`) | - Endpoint: `GET /api/v1/admin/users` หรือ `GET /api/v1/admin/models`<br>- Headers: `Authorization: Bearer <norma...en>` | Module / Feature: Admin / Authorization Check<br>Test Type: Security / Negative | 1. ใช้ Token ของผู้ใช้ธรรมดาเรียก Endpoint ของ Admin | 1. ระบบปฏิเสธคำขอทันทีด้วย HTTP Status Code: `403 Forbidden`<br>2. Response Body: `{"detail": "Super Admin access required"}`<br>3. ข้อมูลรายชื่อผู้ใช้และระบบจัดการโมเดลไม่ถูกเปิดเผย<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_admin.py` | To Do | P0 (Blocker) |
| TC-BE-MODEL-01 | การสลับสถานะโมเดล Active ภายใต้งานฐานข้อมูลเดียว (Atomic Model Deployment) | FR-ADM-04 | 1. มีโมเดล SegFormer เวอร์ชัน `v1.0.0` (Active) และ `v1.0.1` (Inactive) ในตาราง `model_versions`<br>2. ล็อกอินด้วยบัญชีแอดมิน | - Endpoint: `POST /api/v1/admin/models/{model_id}/deploy`<br>- Body: `{"reason": "เลื่อนเวอร์ชันที่ผ่านการทดสอบขึ้นใช้งานจริง"}` | Module / Feature: Admin / Model Registry<br>Test Type: Concurrency / Integration | 1. ส่งคำขอ Deploy โมเดล `v1.0.1` โดยระบุ `model_id` (int) ของเวอร์ชันเป้าหมายพร้อมเหตุผล<br>2. ตรวจสอบตาราง `model_versions` และ `audit_log` | 1. ได้รับ HTTP Status Code: `200 OK`<br>2. กระบวนการรันภายใต้ Transaction แบบ Atomic โดยไม่มีการล็อกแถวแยกต่างหาก<br>3. โมเดลเดิมถูกปลดสถานะเป็น `is_active = false` และโมเดลใหม่กลายเป็น `is_active = true`<br>4. มีการบันทึกประวัติการกระทำลงในตาราง `audit_log` เอกพจน์ทันที<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_admin.py` | To Do | P1 (High) |
| TC-BE-ADMIN-04 | การรับเรื่องและตัดสินรายงานข้อร้องเรียน (Report Moderation) | FR-ADM-02 | 1. ล็อกอินด้วยบัญชี Admin ที่มี `is_superadmin` เป็นจริง<br>2. มีรายงานของผู้ใช้สถานะ `pending` พร้อมเลข `version` ปัจจุบัน | - Endpoint: `POST /api/v1/admin/reports/{report_id}/review` พร้อม `{"version": 1}`<br>- Endpoint: `PATCH /api/v1/admin/reports/{report_id}` | Module / Feature: Admin / Report Review and Decision<br>Test Type: Functional | 1. เรียกดูรายละเอียดด้วย `GET /api/v1/admin/reports/{report_id}`<br>2. กดรับเรื่องด้วย `POST /review` โดยส่งเลข `version` ปัจจุบัน สถานะต้องเปลี่ยนเป็น `reviewing`<br>3. ตัดสินรายงานด้วย `PATCH` แล้วตรวจตาราง `audit_log` | 1. กดรับเรื่องสำเร็จ สถานะเป็น `reviewing` ตัวพิมพ์เล็ก เลข `version` เพิ่มขึ้น<br>2. ส่งเลข `version` เก่าซ้ำได้ `409 Conflict` พร้อม `{"detail": "Conflict: Report has been updated by another user"}`<br>3. การตัดสินบันทึกผลต่าง `status` และ `version` ก่อนหลังลงตาราง `audit_log` เอกพจน์<br>4. Token ผู้ใช้ทั่วไปเรียกแล้วได้ `403 Forbidden` พร้อม `{"detail": "Super Admin access required"}`<br>- **Automation Mapping**: `server/tests/api/test_admin_reports.py` | To Do | P1 (High) |

---

## 6. หมวดหมู่ประวัติและรายงานของผู้ใช้ (History Plus User Reports)

| Test Case ID | Test Case name | Requirement ID | Preconditions | Test Data | Description | Test Steps | Expected Results | Actual Result / Status | Priority |
|---|---|---|---|---|---|---|---|---|---|
| TC-BE-HIST-01 | การดึงรายการประวัติการสแกน (History List) | FR-HIST-01 | 1. ผู้ใช้มีประวัติการสแกนอย่างน้อย 2 รายการ | - Endpoint: `GET /api/v1/history?page=1&limit=20`<br>- Headers: `Authorization: Bearer <valid...en>` | Module / Feature: History / List Scans<br>Test Type: Functional | 1. เรียกดูรายการประวัติพร้อม Token ที่ถูกต้อง<br>2. ตรวจสอบการเรียงลำดับและฟิลด์ที่ได้ | 1. ตอบ `200 OK` พร้อม `items`, `total`, `page`, `limit`<br>2. รายการเรียงจากใหม่ไปเก่า มี `scan_id`, `risk_score`, `risk_level` ตัวพิมพ์เล็ก, `status`, `created_at`, `title`<br>3. เวลาเป็นเขตเวลาไทย UTC+7<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_history.py` | To Do | P1 (High) |
| TC-BE-HIST-02A | การดูรายละเอียดประวัติรายฉบับ (History Detail) | FR-HIST-02 | 1. มี scan_id ที่เป็นของผู้ใช้เอง | - Endpoint: `GET /api/v1/history/{scan_id}` | Module / Feature: History / Detail<br>Test Type: Functional | 1. เรียกดูรายละเอียดด้วย `GET /api/v1/history/{scan_id}`<br>2. ตรวจฟิลด์และเจ้าของงาน | 1. ตอบ `200 OK` พร้อมข้อมูลตรงกับเจ้าของงาน (`scan_id`, `risk_score`, `risk_level` ตัวพิมพ์เล็ก, `status`, `created_at`, `title`)<br>2. เปิดงานของผู้อื่นได้ `403 Forbidden` พร้อม `{"detail": "Not authorized to view this scan"}`<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_history.py` | To Do | P1 (High) |
| TC-BE-HIST-02B | การลบประวัติและการยืนยันว่าไม่พบ (History Delete Verify) | FR-HIST-03 | 1. reseed ใน TC เอง: อัปโหลดภาพทดสอบ 1 ไฟล์ รอจน `completed` จด `scan_id` และ `image_hash` (ห้ามอ้าง scan_id จาก TC-BE-HIST-02A) | - Endpoint: `DELETE /api/v1/history/{scan_id}`<br>- Endpoint: `GET /api/v1/history/{scan_id}` | Module / Feature: History / Delete<br>Test Type: Functional | 1. ลบด้วย `DELETE /api/v1/history/{scan_id}`<br>2. เรียกดูซ้ำด้วย `GET` เดิม<br>3. ตรวจไฟล์ภาพต้นฉบับและ Heatmap บนดิสก์ตามเงื่อนไข `image_hash` ร่วม | 1. `DELETE` ตอบสำเร็จ และลบไฟล์ภาพเมื่อไม่มีงานอื่นใช้ `image_hash` เดียวกัน (หากมีงานอื่นใช้ร่วม ไฟล์ต้องคงอยู่)<br>2. `GET` ซ้ำตอบ `404 Not Found` พร้อม `{"detail": "Scan not found"}`<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_history.py` | To Do | P1 (High) |
| TC-BE-RPT-01 | การส่งรายงานและการดึงหมวดหมู่กับรายงานของตนเอง (User Reports) | FR-RPT-01 | 1. มี scan_id ที่เป็นของผู้ใช้เองและยังไม่เคยถูกรายงาน | - Endpoint: `POST /api/v1/reports` พร้อม `scan_id`, `category` เป็น `fake_slip`, `description` ยาวไม่ต่ำกว่า 10 ตัวอักษร<br>- Endpoint: `GET /api/v1/reports/categories`<br>- Endpoint: `GET /api/v1/reports/my` | Module / Feature: User Reports / Submit and Query<br>Test Type: Functional | 1. ดึงหมวดหมู่จาก `GET /api/v1/reports/categories`<br>2. ส่งรายงานด้วย `POST /api/v1/reports`<br>3. ดึงรายงานของตนเองด้วย `GET /api/v1/reports/my` | 1. หมวดหมู่มี 7 ค่า ได้แก่ `romance_scam`, `online_shopping`, `fake_slip`, `investment`, `identity_theft`, `ai_deepfake`, `other`<br>2. ส่งรายงานสำเร็จได้ `201 Created` พร้อมสถานะ `pending` ตัวพิมพ์เล็ก<br>3. ส่งงานเดิมซ้ำได้ `409 Conflict` พร้อม `{"detail": "This scan has already been reported"}` ส่วนการรายงานงานที่ไม่ใช่ของตนเองได้ `403 Forbidden`<br>4. `GET /my` แสดงรายงานของตนเองพร้อมสถานะและเลข `version`<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_history.py` | To Do | P1 (High) |

---

## 7. หมวดหมู่การบริหาร Admin ขั้นสูง (Admin Health Search Sessions Audit)

| Test Case ID | Test Case name | Requirement ID | Preconditions | Test Data | Description | Test Steps | Expected Results | Actual Result / Status | Priority |
|---|---|---|---|---|---|---|---|---|---|
| TC-BE-ADMIN-02 | การตรวจสุขภาพระบบและการค้นหาข้ามระบบ (Admin Health Plus Search) | FR-ADM-01 | 1. ล็อกอินด้วยบัญชี Admin ที่มี `is_superadmin` เป็นจริง | - Endpoint: `GET /api/v1/admin/health`<br>- Endpoint: `GET /api/v1/admin/search?q=slip` | Module / Feature: Admin / Health and Global Search<br>Test Type: Functional | 1. เรียกดูสถานะสุขภาพระบบ<br>2. ค้นหาด้วยคำค้นแล้วตรวจผลลัพธ์ | 1. `GET /api/v1/admin/health` ตอบ `200 OK` พร้อมฟิลด์ `database`, `storage`, `models`, `queue` (ไม่มีฟิลด์ชื่อแคชหรือ Worker โดยตรง คิวงานดูจาก `queue` และโมเดลดูจาก `models`)<br>2. `GET /search` ตอบ `200 OK` พร้อมผลลัพธ์ที่ตรงกับคำค้น<br>3. Token ผู้ใช้ทั่วไปเรียกแล้วได้ `403 Forbidden`<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_admin.py` | To Do | P2 (Medium) |
| TC-BE-ADMIN-03 | การดูและเพิกถอน Session ของ Admin (Admin Sessions) | FR-ADM-01 | 1. ล็อกอิน Admin สำเร็จมี Session ปัจจุบัน | - Endpoint: `GET /api/v1/admin/sessions`<br>- Endpoint: `POST /api/v1/admin/sessions/{session_id}/revoke`<br>- Endpoint: `GET /api/v1/admin/me` | Module / Feature: Admin / Session Management<br>Test Type: Security | 1. ดึงรายการ Session ทั้งหมดของตนเอง<br>2. เพิกถอน Session ที่สร้างเพื่อทดสอบ<br>3. นำ Refresh Token เดิมไปเรียก `POST /api/v1/admin/refresh` | 1. รายการ Session แสดงรหัส วันหมดอายุ และการใช้งานล่าสุด<br>2. เพิกถอนสำเร็จและ Session ที่ถูกเพิกถอนใช้งานต่อไม่ได้<br>3. Refresh Token ที่ถูกเพิกถอนตอบ `401 Unauthorized`<br>- **Automation Mapping**: `server/tests/api/test_admin_auth.py` | To Do | P1 (High) |
| TC-BE-AUDIT-01 | การกรองบันทึก Audit Log และการแสดงผลต่าง JSON (Audit Filter) | FR-ADM-06, FR-AUDIT-01 | 1. มีประวัติการ Deploy โมเดลหรือตัดสินรายงานในตาราง `audit_log` | - Endpoint: `GET /api/v1/admin/audit-logs?page=1&limit=50&search=&action=&entity_type=` | Module / Feature: Admin / Audit Log Viewer<br>Test Type: Functional | 1. ดึงบันทึกแบบไม่กรอง<br>2. กรองด้วย `action` และ `entity_type`<br>3. ตรวจสอบฟิลด์ผลต่างของรายการที่เปลี่ยนสถานะ | 1. ตอบ `200 OK` พร้อม `items`, `total`, `page`, `limit`<br>2. แต่ละรายการมี `action`, `entity_type`, `entity_id`, `before_state`, `after_state`<br>3. การเปลี่ยนสถานะรายงานแสดงผลต่าง `status` และ `version` ก่อนหลังถูกต้อง<br>4. ตารางที่ใช้คือ `audit_log` เอกพจน์<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_admin.py` | To Do | P1 (High) |

---

## 8. หมวดหมู่ความสอดคล้องของฐานข้อมูลและเวลา (Database Integrity & Timezone)

| Test Case ID | Test Case name | Requirement ID | Preconditions | Test Data | Description | Test Steps | Expected Results | Actual Result / Status | Priority |
|---|---|---|---|---|---|---|---|---|---|
| TC-BE-DB-01 | การบันทึกเวลาเป็นเขตเวลาประเทศไทย UTC+7 (Timezone Verification) | NFR-SYS-01 | 1. PostgreSQL Container และระบบ FastAPI ตั้งค่า Timezone ตรงกัน | การสร้าง Record ใหม่ในตาราง `users`, `scans`, หรือ `audit_log` | Module / Feature: Database / Timezone Consistency<br>Test Type: Functional / Data Integrity | 1. ส่งคำขอสร้างข้อมูลใหม่<br>2. ตรวจสอบค่าในคอลัมน์ `created_at` ใน PostgreSQL | 1. ค่าเวลา `created_at` ตรงกับเวลาจริงของประเทศไทย (`Asia/Bangkok` หรือ `UTC+7`)<br>2. ไม่เกิดปัญหาเวลาเลื่อนถอยหลังไป 7 ชั่วโมง (UTC Offset 0)<br>- **Automation Mapping**: `server/tests/check_time_tz.py` | To Do | P1 (High) |

---

## 9. หมวดหมู่การตรวจสอบข้อมูลนำเข้าและสิทธิ์ผิดพลาด (Negative Auth & API Errors)

| Test Case ID | Test Case name | Requirement ID | Preconditions | Test Data | Description | Test Steps | Expected Results | Actual Result / Status | Priority |
|---|---|---|---|---|---|---|---|---|---|
| TC-BE-AUTH-07 | การปฏิเสธข้อมูลสมัครสมาชิกไม่ครบ รูปแบบผิด อีเมลซ้ำ และฟิลด์บทบาทแปลกปลอม (Register Negative Plus Duplicate) | FR-AUTH-01 | 1. PostgreSQL และ FastAPI ทำงานปกติ<br>2. มีผู้ใช้ `be_test_user@scamguard.local` อยู่แล้ว | - Endpoint: `POST /api/v1/auth/register`<br>- เคส body ว่าง: `{}`<br>- เคสอีเมลผิดรูปแบบ: `{"email": "not-an-email", "password": "StrongPassword123!", "full_name": "Test"}`<br>- เคสไม่ส่ง `full_name`<br>- เคสอีเมลซ้ำ: อีเมล `be_test_user@scamguard.local` พร้อมรหัสผ่านและชื่อครบ<br>- เคสฟิลด์แปลกปลอม: body ครบถ้วนบวก `"role": "admin"`<br>- เคสรหัสผ่านสั้น: `"password": "abc"` | Module / Feature: Auth / Register Validation and Duplicate Handling<br>Test Type: Functional / API | 1. ยิงทีละเคสไปยัง `POST /api/v1/auth/register`<br>2. ตรวจรหัสสถานะ ข้อความตอบกลับ และแถวในตาราง `users` กับ `consent_logs` | 1. body ว่าง อีเมลผิดรูปแบบ และไม่ส่ง `full_name` ได้ `422 Unprocessable Entity` และไม่มีแถวใหม่ในตาราง `users`<br>2. อีเมลซ้ำได้ `400 Bad Request` พร้อม `{"detail": "Email already registered"}` (หมายเหตุ: ปลายทางนี้ใช้ 400 ไม่ใช่ 409)<br>3. ฟิลด์ `role` ที่ส่งมาเพิ่มถูกเพิกเฉย บัญชีใหม่มี `role` เป็น `user` เสมอ และไม่มี Plaintext Password ใน Response<br>4. รหัสผ่านสั้น (เช่น `abc` ต่ำกว่า 8 ตัวอักษร) ต้องถูกปฏิเสธด้วย `422 Unprocessable Entity` ตามเกณฑ์ความยาวขั้นต่ำฝั่ง Server (min-length 8) และไม่มีแถวใหม่ในตาราง `users`<br>5. ไม่ส่ง `system_consent` และ `research_consent` ระบบบันทึกค่าเริ่มต้น (`system_consent` เป็นจริง `research_consent` เป็นเท็จ) ลงตาราง `consent_logs`<br>- **Defect Log (ยังไม่ผ่าน — ห้ามรับรองเป็น pass)**:<br>- `DEFECT-BE-AUTH-07`: พฤติกรรมปัจจุบันยังสร้างบัญชีสำเร็จ (`201 Created`) เพราะฝั่งบริการไม่มีเกณฑ์ความยาวขั้นต่ำ (ตรวจเฉพาะฝั่งแอป) — เป็น normalization of deviance ฝั่งความปลอดภัย TC ข้อนี้คงสถานะ FAIL จนกว่าจะเพิ่ม min-length ฝั่ง Server<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_auth_flow.py` | To Do | P1 (High) |
| TC-BE-API-01 | การปฏิเสธ Token หมดอายุ รูปแบบผิด และบทบาทไม่ตรงงาน (Expired Token Plus Wrong Role) | NFR-SEC-02, NFR-SEC-03 | 1. มี Token ผู้ใช้ทั่วไปที่ถูกต้อง 1 ชุด และ Token หมดอายุ 1 ชุด<br>2. มี `scan_id` ของผู้ใช้อีกคนหนึ่งราย | - Endpoint: `GET /api/v1/history`, `GET /api/v1/scan/{scan_id}`, `POST /api/v1/reports`, `GET /api/v1/admin/users`<br>- Header: `Authorization: Bearer <expir...en>` และ `Bearer <normal_user_token>` | Module / Feature: Auth / Cross-endpoint Authorization<br>Test Type: API | 1. เรียก `GET /api/v1/history` ด้วย Token หมดอายุและด้วย Token รูปแบบผิด<br>2. เรียก `GET /api/v1/scan/{scan_id}` ของผู้ใช้คนอื่นด้วย Token ผู้ใช้ทั่วไป<br>3. ส่ง `POST /api/v1/reports` อ้าง `scan_id` ของผู้ใช้คนอื่น<br>4. เรียก `GET /api/v1/admin/users` ด้วย Token ผู้ใช้ทั่วไป | 1. Token หมดอายุหรือรูปแบบผิดเรียก `GET /api/v1/history` ได้ `401 Unauthorized` พร้อม `{"detail": "Could not validate credentials"}`<br>2. เปิดงานสแกนของผู้อื่นได้ `403 Forbidden` พร้อม `{"detail": "Not authorized to view this scan"}`<br>3. รายงานงานของผู้อื่นได้ `403 Forbidden` พร้อม `{"detail": "Scan not found or you don't have permission"}`<br>4. ผู้ใช้ทั่วไปเรียกปลายทางแอดมินได้ `403 Forbidden` พร้อม `{"detail": "Super Admin access required"}`<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_admin.py` | To Do | P0 (Blocker) |

---

## 10. หมวดหมู่การป้องกันข้อมูลอันตราย (Injection Safety)

| Test Case ID | Test Case name | Requirement ID | Preconditions | Test Data | Description | Test Steps | Expected Results | Actual Result / Status | Priority |
|---|---|---|---|---|---|---|---|---|---|
| TC-BE-SEC-01 | การรับข้อความ SQLi และ XSS ในชื่อเรื่อง คำค้น รายงาน และฟิลด์สมัคร/ล็อกอิน (SQLi Plus XSS incl. Auth Fields) | NFR-SEC-04 | 1. มี Bearer Token ที่ถูกต้อง<br>2. มีภาพตัวอย่างขนาดไม่เกิน 20MB | - Endpoint: `POST /api/v1/scan/` ฟิลด์ฟอร์ม `title` เป็น `' OR '1'='1` และ `<script>alert(1)</script>`<br>- Endpoint: `GET /api/v1/history?keyword=<script>alert(1)</script>`<br>- Endpoint: `POST /api/v1/reports` ฟิลด์ `description` (ยาวเกิน 10 ตัวอักษร) เป็น `<img src=x onerror=alert(1)> สลิปปลอมยอดเงิน`<br>- Endpoint: `POST /api/v1/auth/register` ฟิลด์ `full_name` เป็น `<script>alert(1)</script>` และฟิลด์ `email` เป็น `x'OR'1'='1@e.com` (รูปแบบผ่าน validation ได้) ควบคู่กับเคส `' OR '1'='1`<br>- Endpoint: `POST /api/v1/auth/login` ฟิลด์ฟอร์ม `username`/`password` เป็น `' OR '1'='1` และ `<script>alert(1)</script>` | Module / Feature: Security / Untrusted Text in Title Keyword and Description<br>Test Type: Security | 1. อัปโหลดภาพพร้อม `title` ที่เป็นข้อความโจมตี แล้วเปิดดูประวัติ<br>2. ค้นหาประวัติด้วย `keyword` ที่เป็นข้อความโจมตี<br>3. ส่งรายงานด้วย `description` ที่ฝังแท็ก แล้วดึงรายงานของตนเองด้วย `GET /api/v1/reports/my`<br>4. ยิง `POST /api/v1/auth/register` ด้วย payload โจมตี แล้วตรวจว่าไม่มีบัญชีผิดปกติและไม่มี error 500 จากนั้นยิง `POST /api/v1/auth/login` ด้วยข้อความโจมตี ตรวจว่าไม่หลุดล็อกอิน | 1. ทุกคำขอผ่านตามปกติ (`200 OK` หรือ `201 Created`) โดยข้อความโจมตีถูกเก็บและแสดงเป็นตัวอักษรธรรมดา ไม่รันสคริปต์<br>2. การค้นหาไม่เกิดข้อผิดพลาดฐานข้อมูล ไม่คืนข้อมูลของผู้อื่น ไม่หลุดโครงสร้างตาราง<br>3. ไม่มีไฟล์อันตรายถูกเขียนลงดิสก์เพิ่มนอกเหนือภาพหลักฐานปกติ และไม่มีบันทึกผิดปกติในตาราง `audit_log`<br>4. ฟิลด์สมัคร/ล็อกอิน: payload โจมตีต้องไม่ทำให้เกิด SQL error/500 และไม่หลุดล็อกอินโดยไม่ใช้รหัสผ่านที่ถูกต้อง (login ผิดตอบ `401` หรือ `422` ตามปกติ) ข้อความที่เก็บต้องแสดงเป็นตัวอักษรธรรมดา ไม่รันสคริปต์<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_scan_workflow.py` | To Do | P0 (Blocker) |

---

## 11. หมวดหมู่ความสัมพันธ์ข้อมูล (Database Relations)

| Test Case ID | Test Case name | Requirement ID | Preconditions | Test Data | Description | Test Steps | Expected Results | Actual Result / Status | Priority |
|---|---|---|---|---|---|---|---|---|---|
| TC-BE-DB-02 | การคงความสัมพันธ์เมื่อลบผู้ใช้ (งานสแกน รายงาน บันทึกยินยอม) (FK Delete Behavior) | FR-HIST-03, NFR-PDPA-02 | 1. สร้างผู้ใช้ทดสอบที่มีงานสแกน 1 งาน รายงาน 1 ฉบับ และบันทึกยินยอม 1 แถว<br>2. จด `user_id`, `scan_id`, ค่า `image_hash` ไว้ | - ตาราง `users`, `scans`, `scam_reports`, `consent_logs` | Module / Feature: Database / Foreign Key Delete Rules<br>Test Type: Database | 1. ลบแถวผู้ใช้ทดสอบออกจากตาราง `users`<br>2. ค้นตาราง `scans` ด้วย `image_hash` เดิม<br>3. ค้นตาราง `scam_reports` และ `consent_logs` ของผู้ใช้นั้น | 1. แถวงานสแกนยังคงอยู่ โดย `user_id` กลายเป็นค่าว่าง (ไม่ถูกลบตามผู้ใช้)<br>2. แถวรายงานยังคงอยู่ โดย `user_id` กลายเป็นค่าว่าง (ไม่ถูกลบตามผู้ใช้)<br>3. บันทึกยินยอมของผู้ใช้ถูกลบตามผู้ใช้จนหมด (ลบแบบพ่วง)<br>- **Automation Mapping**: Manual DB Verification | To Do | P1 (High) |
| TC-BE-DB-03 | การสแกนภาพซ้ำรหัสแฮชเดิมและการลบไฟล์เมื่อไม่มีงานใดใช้แล้ว (Duplicate Hash Plus Shared File Delete) | FR-INPUT-03, FR-HIST-03 | 1. มี Bearer Token ที่ถูกต้อง<br>2. มีภาพตัวอย่าง 1 ไฟล์ | - Endpoint: `POST /api/v1/scan/`, `DELETE /api/v1/history/{scan_id}`, `GET /api/v1/history/{scan_id}`<br>- อัปโหลดไฟล์เดิมซ้ำ 2 ครั้ง | Module / Feature: Database / Duplicate Image Hash Handling<br>Test Type: Database | 1. อัปโหลดไฟล์เดิม 2 ครั้ง จด `scan_id` ทั้งสองและค่า `image_hash`<br>2. ลบงานแรกด้วย `DELETE /api/v1/history/{scan_id}` แล้วตรวจไฟล์ภาพต้นฉบับและ Heatmap<br>3. ลบงานที่สอง แล้วเรียกดูงานที่ลบด้วย `GET` ซ้ำ | 1. ทั้งสองครั้งสำเร็จ (`200 OK`) ได้งานคนละรหัสแต่ `image_hash` เดียวกัน (คอลัมน์นี้เป็นดัชนีค้นหา ไม่ใช่ค่าห้ามซ้ำ)<br>2. ลบงานแรกแล้วไฟล์ภาพยังอยู่ เพราะยังมีอีกงานใช้รหัสแฮชเดียวกัน<br>3. ลบงานที่สองแล้วไฟล์ถูกลบออกจากดิสก์ และเรียกดูซ้ำได้ `404 Not Found` พร้อม `{"detail": "Scan not found"}`<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_history.py` | To Do | P1 (High) |

---

## 12. หมวดหมู่การเชื่อมงานข้ามบริการ (Cross-service Integration)

| Test Case ID | Test Case name | Requirement ID | Preconditions | Test Data | Description | Test Steps | Expected Results | Actual Result / Status | Priority |
|---|---|---|---|---|---|---|---|---|---|
| TC-BE-INT-01 | โซ่สแกน ประวัติ รายงาน ตั้งแต่ต้นจนจบ (Scan to History to Report Chain) | FR-INPUT-03, FR-HIST-01, FR-RPT-01 | 1. มี Bearer Token ที่ถูกต้อง<br>2. มีภาพตัวอย่างที่ไม่เคยรายงานมาก่อน | - Endpoint: `POST /api/v1/scan/` พร้อม `file` และ `title` ว่า `สลิปทดสอบโซ่`<br>- Endpoint: `GET /api/v1/history?page=1&limit=20`<br>- Endpoint: `GET /api/v1/scan/{scan_id}`<br>- Endpoint: `POST /api/v1/reports` พร้อม `scan_id`, `category` เป็น `fake_slip`, `description` ยาวเกิน 10 ตัวอักษร<br>- Endpoint: `GET /api/v1/reports/my` | Module / Feature: Integration / Scan History Report Flow<br>Test Type: Integration | 1. อัปโหลดภาพแล้วรอจน `GET /api/v1/scan/{scan_id}` มีสถานะ `completed`<br>2. เปิด `GET /api/v1/history` ตรวจว่างานใหม่ปรากฏรายการแรก<br>3. ส่งรายงานของงานนั้น แล้วดึง `GET /api/v1/reports/my` | 1. งานสแกนจบด้วยสถานะตัวพิมพ์เล็ก `completed` มีคะแนน 3 ด้าน `text_score`, `visual_score`, `source_score` และ `risk_grade` เป็น `low`, `medium` หรือ `high`<br>2. ประวัติแสดงงานใหม่เป็นรายการแรก มี `scan_id`, `risk_score`, `risk_level` ตัวพิมพ์เล็ก, `status`, `created_at`, `title` ตรงกัน<br>3. ส่งรายงานสำเร็จได้ `201 Created` สถานะ `pending` ตัวพิมพ์เล็ก และพบรายงานใน `GET /my` ของผู้ใช้คนเดียวกัน<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_history.py` | To Do | P1 (High) |

---

## 13. หมวดหมู่การถดถอยหลังเปลี่ยนโมเดล (Post-deploy Regression)

| Test Case ID | Test Case name | Requirement ID | Preconditions | Test Data | Description | Test Steps | Expected Results | Actual Result / Status | Priority |
|---|---|---|---|---|---|---|---|---|---|
| TC-BE-REG-01 | ข้อมูลเดิมยังอ่านได้และเกณฑ์เสี่ยงไม่เปลี่ยนหลัง Deploy โมเดล (History Readable After Deploy) | FR-ADM-04, FR-HIST-01 | 1. ล็อกอินแอดมินที่มี `is_superadmin` เป็นจริง<br>2. มีประวัติสแกนเดิมของผู้ใช้ทั่วไปอย่างน้อย 2 งาน พร้อมจด `scan_id`, `total_risk_score`, `risk_grade`<br>3. มีโมเดลเป้าหมายในตาราง `model_versions` พร้อม `model_id` (int) | - Endpoint: `POST /api/v1/admin/models/{model_id}/deploy` พร้อมเหตุผล<br>- Endpoint: `GET /api/v1/history`, `GET /api/v1/history/{scan_id}`, `POST /api/v1/scan/`<br>- เกณฑ์เดิม: 0-39 เป็น `low`, 40-69 เป็น `medium`, 70-100 เป็น `high` | Module / Feature: Regression / Model Deploy Backward Compatibility<br>Test Type: Regression | 1. Deploy โมเดลเป้าหมาย แล้วตรวจตาราง `audit_log`<br>2. เรียกประวัติและรายละเอียดงานเดิมซ้ำ เปรียบเทียบคะแนนและระดับกับค่าที่จดไว้<br>3. สแกนภาพตัวอย่างใหม่อีก 1 ภาพ ตรวจโครงสร้าง Response เดิม | 1. Deploy สำเร็จ (`200 OK`) โมเดลเก่าหมดสถานะ โมเดลใหม่ใช้งาน และมีบันทึกในตาราง `audit_log`<br>2. งานเดิมทุกงานยังอ่านได้ คะแนนและระดับความเสี่ยงเท่าเดิมทุกประการ<br>3. งานสแกนใหม่ยังได้ฟิลด์ครบ (`id`, `total_risk_score`, `risk_grade`, `text_score`, `visual_score`, `source_score`, `heatmap_image_url`, `status`, `progress`) และเกณฑ์ 3 ระดับยังแบ่งที่ 40 กับ 70 เหมือนเดิม<br>- **Automation Mapping**: `tests_all/automate_tests/tests/api/test_admin.py` | To Do | P1 (High) |
