# รายงาน Proofread เอกสารทดสอบ เทียบ ISTQB CTFL + IEEE 829 (QA/Tester)

- วันที่ตรวจ: 2026-09-13 | ขอบเขต: tests_all/**, Document/tests_doc/**, server/tests/tests_model/README.md
- ยอด: Critical 19 | Major 57 | Minor 18 = 94 จุด (ตรวจอย่างเดียว ยังไม่แก้)

## T1a — manual mobile/backend/e2e (C8/M13/m4)

### Critical
| # | ไฟล์:บรรทัด | ประเด็น | วิธีแก้ |
|---|---|---|---|
| 1 | mobile:202 | เอา GAP (ไม่มีจอเตือน 10MB) มาเขียนเป็น expected | แยกเป็น defect log + เขียน expected ที่ต้องเป็นจริง หรือลดขอบเขต TC |
| 2 | mobile:595 | TC ทดสอบ FCM ที่ยังไม่มีอยู่จริง | park TC-MOB-NOTIF-01 เป็น future เหลือเฉพาะพฤติกรรมปัจจุบัน |
| 3 | mobile:326 | TC อ้าง DELETE endpoint ที่ไม่มี | เก็บส่วนทำได้จริง (หยุด polling ฝั่ง client) ส่วน DELETE ย้ายเป็น change request |
| 4 | backend:594 | รหัสสั้นได้ 201 (normalization of deviance) | กำหนด min-length ฝั่ง server (เช่น 8) expected เป็น 422 + เปิด defect |
| 5 | backend:233 | steps ไม่มีขั้น poll แต่ expected อ้าง failed ผ่าน GET | เพิ่ม steps จด scan_id + poll จน failed ใน 60s (BE-SCAN-03/04) |
| 6 | e2e:31 | TC เดียวผูก 18 requirements | แยกตามกลุ่ม (upload/AI/result/history) + trace ราย requirement |
| 7 | e2e:168 | expected บันทึก defect 403-ban แทนพฤติกรรม | กำหนดสเปก 403 ก่อน (auto-logout+message) แล้วเขียน expected ตามนั้น |
| 8 | e2e:193 | oracle ขัดกันเองเรื่องแบนเนอร์ออฟไลน์ | เลือก oracle เดียวตามสเปกจริง |

### Major
| ไฟล์:บรรทัด | ประเด็น | วิธีแก้ |
|---|---|---|
| mobile:58 | expected ไม่มี time bound + ตรวจ token ด้วยตาไม่ได้ | เติม ≤2s; white-box check ย้ายเป็น automated |
| mobile:137 | ไม่บอกวิธีบังคับ refresh หมดอายุ | ระบุ 1 วิธี (fixture/แก้เวลา/TTL) |
| mobile:162 | คมชัด/ไม่บิดเบี้ยว (subjective) | เต็มกรอบ preview ไม่ stretch อัตราส่วนคงเดิม |
| mobile:277 | precondition OR 2 สถานการณ์ | แยก 2 TC (500 vs no-network) + วิธีจำลอง |
| mobile:302 | timeout ไม่มีค่า | ระบุ 60s + วิธีจับเวลา + เกณฑ์หยุด polling |
| mobile:620 | TC เดียวรวม 6 เคสสมัคร | แยกย่อย/table-driven + error message รายช่อง |
| mobile:761 | ไม่ระบุรุ่นเครื่อง | minSdk/API level + รุ่นอ้างอิง 2 รุ่น |
| mobile:130 | token P2 แต่ backend P0 เรื่องเดียวกัน | ปรับเป็น P1 ขึ้นไป |
| backend:290 | CACHE-02 พึ่งผล CACHE-01 | warm cache ใน TC เอง + วิธีล้าง/เตรียม |
| backend:314 | steps ยิงแค่ admin แต่ expected เคลม 2 โควตา | เพิ่ม steps หรือแยก 2 TC + Retry-After |
| backend:445 | GET+DELETE+GET ใน TC เดียว | แยก detail/delete-verify + reseed |
| e2e:79 | ไม่มี oracle ว่าข้าม GPU | latency P95 / log cache-hit / ไม่มี GPU job + ตัวเลข |
| backend:636 | injection คลุมแค่ scan fields | เพิ่ม SQLi/XSS ผ่าน register/login fields |

### Minor
mobile:427 (100% ไม่นิยาม → ระบุฟิลด์เทียบ), e2e:16 (P1=Critical ขัดสเกล → ใช้ P0-P3 เดียว), mobile:718 (boundary 9/10/11 ไม่ครบ), backend:38 (แฮชมาตรฐาน → bcrypt 60 + คำสั่งตรวจ)

## T1b — manual ai/admin/nfr (C5/M17/m3)

### Critical
| # | ไฟล์:บรรทัด | ประเด็น | วิธีแก้ |
|---|---|---|---|
| 1 | ai_model:313 | TC-AI-ACC-01 ไม่มี dataset/สคริปต์ รันไม่ได้ | ถอดเป็น risk/GAP ระบุ dataset ID + สคริปต์ + เกณฑ์ก่อนเขียน TC ใหม่ |
| 2 | nfr:422 | refresh ใช้ต่อได้หลัง logout เขียนเป็น pass | expected ต้อง 401 หรือเปิด risk acceptance แยก |
| 3 | nfr:355 | locust ใช้ไม่ได้ (401) แต่ TC ยัง executable | เลื่อน PERF-04/05 เป็น blocked + งานขยายสคริปต์ |
| 4 | nfr:119 | ไฟล์อันตรายตอบ 200 ก่อนค่อย failed | sync reject 400/422 หรือ poll ใน 60s + risk acceptance |
| 5 | admin:65 | steps ยิง login แต่ expected โควตา refresh | แยก 4 TC ย่อย + แก้ step 4 ยิง refresh เกิน 5 ครั้ง |

### Major (ย่อ)
ai:28 (patch count ไม่ระบุ → stride=448 + จำนวนคาดหวัง 1920×1080); ai:69 (CER ไม่มีสูตร → ground-truth + สคริปต์ + blank negative); ai:83 (input ลอย → ตรึงพจนานุกรม + ประโยคคงที่ + clean-text negative); ai:110 (XAI ไม่มี seed → model commit/prompt/temp=0/blind scoring); ai:209 (8K ไม่มีไฟล์จริง → มี 8K จริงหรือลดเหลือ 4K + P1?); ai:274 (noise ไม่นิยาม → ตรึงไฟล์ + เพดานตัวเลข); ai:381 (4 เคสใน TC เดียว → แยก 4 TC + ทบทวน EXIF vs PDPA); ai:411 (relative ไม่มี baseline → ตรึงไฟล์ 15/30° + blur kernel + hash); ai:436 (location ไม่มี metric → IoU/center-distance + mapping table); ai:175 (ไม่มี cap/boundary → base95+bonus ตรวจ cap100 + ขอบ 39/40); admin:24 (fixture ลอย + token เก็บ 3 ที่ขัดกัน → seed + memory+HttpOnly); admin:113 (live DB → fixture seed + COUNT เทียบ); admin:478 (lifecycle รวมก้อน → แยก 4 TC + timeout + 404); nfr:45 (n=10 ประเมิน p99 ไม่ได้ → N≥100 หรือเหลือ P50/P95); admin:161 (ทันที ไม่มีเวลา + token ใน query → ≤3s + ย้าย header); nfr:152 (expired token ไม่ใช้ + Secure Storage ตรวจไม่ได้ → เพิ่ม step 401 + ย้าย assertion); nfr:184 (ไม่มี Decline → เพิ่มเคส + ตรวจ consent_logs UTC+7)

### Minor
nfr:63 (taxonomy P1/Critical ปน → สเกลเดียวทั้งโปรเจกต์); admin:427 (page=0+limit=0 พร้อมกัน → แยกเคส); ai:49 (±0.05 ซ้ำ HEAT-01 → ยุบหรือสกัด threshold กลาง)

## T2 — rtm/signoff/bug/reports (C2/M16/m6)

### Critical
| # | ไฟล์:บรรทัด | ประเด็น | วิธีแก้ |
|---|---|---|---|
| 1 | release_signoff:33 | exit criteria 5 ข้อประเมินไม่ได้ (execution_log ไม่มีผลจริง) | เพิ่ม pre-sign gate: ต้องมีผลรันจริงก่อนประเมิน ห้ามลงนามบนแม่แบบเปล่า |
| 2 | tests_report/README:25 | admin/+model/ มีแค่ .gitkeep ไม่มี owner/due | กำหนด owner + due + ผูกเป็นเงื่อนไขบล็อกปล่อย |

### Major (ย่อ)
rtm:186 (trace ทิศเดียว → ตาราง TC-to-Req ถาวร + orphan-scan อัตโนมัติ); rtm:275 (นับ Partial รวม → แยก Covered เต็ม; FR เต็มจริง 26/39=66.7%); rtm:17 (baseline 2 ชุด + ย้ายเลข → freeze ชุดเดียว); rtm:35 (ไม่มี evidence link → คอลัมน์ไฟล์รายงาน+วันที่+pass/fail); signoff:50 (ข้อ 5 ไม่มีตัวเลข → GAP P0/P1=0 + coverage ขั้นต่ำ); signoff:66 (ขาดทั้ง component ยอมรับเป็นข้อจำกัดได้ → เปลี่ยนเป็นบล็อกหรือ compensating evidence); bug_template:99 (ไม่มีช่อง root cause structured → §2.9 defect type/phase); tests_report/README:7 (ดัชนีไม่มี metric → ตารางรอบล่าสุด + pass rate); automate README:35 (แม่แบบไม่มี TC/Req/commit/env → เพิ่มช่องบังคับ); :41 (ไม่มี trend → รอบสรุปรวม + flaky tracking); automate_tests/README:86 (ไม่มี strategy → เกณฑ์เลือกเคส/coverage/flaky/owner); :72 (fixtures/images ว่าง → จัดชุดภาพมาตรฐาน); :106 (locust ไม่เก็บ percentile → แยก hit/miss + P50/P95/P99); :105 (ไม่มี Admin/Model suite + CI ไม่มี gate → smoke suite + quality gate); tests_model README:17 (n=2 ไม่มี metric/threshold → ตาราง metric + threshold ≥85% + repro); :13 (ไม่ผูก TC/Req → header mapping NFR-AI/FR-SYS-05)

### Minor (ย่อ)
rtm:266 (Android 10–14 vs v1 → ระบุชัด); rtm:275 (ฐานนับ 26/39 vs 28/41 → สูตรเดียว + แก้ PARETO); bug:80 (ไม่มีหลักฐานได้แค่เขียนเหตุผล → approver + เกณฑ์ขั้นต่ำ); bug:94 (lifecycle ไม่ครบ → Rejected/Reopened/Duplicate-of); bug:26 (ไม่มี phase/SLA → เพิ่มช่อง + SLA); tests_model:25 (ไม่มี hash/คำสั่ง → repro info)

## T3 — test plans Document/tests_doc (C4/M11/m5)

### Critical
| # | ไฟล์:บรรทัด | ประเด็น | วิธีแก้ |
|---|---|---|---|
| 1 | plan README:240-242 | ไม่ม
...[truncated 2085 chars]